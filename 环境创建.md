# 一、项目整体目录先创建

建议先创建项目根目录：

```
mkdir Take_medicineAPP
cd Take_medicineAPP
```

项目结构建议是：

```
Take_medicineAPP/
├── backend/              # FastAPI 后端服务
├── frontend/             # uni-app + Vue 3 Android 前端
├── docs/                 # 项目文档、接口说明、ER 图、测试报告
├── environment.yml       # 后端 conda 环境
├── docker-compose.yml    # MySQL 数据库环境
└── .env.example          # 环境变量示例
```

------

# 二、创建后端 Conda 环境

## 1. 创建 `environment.yml`

在项目根目录下创建：

```
environment.yml
```

内容可以先这样写：

```
name: take-medicine-api

channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.11
  - pip

  - pip:
      - fastapi
      - uvicorn[standard]
      - sqlalchemy
      - alembic
      - pymysql
      - pydantic
      - pydantic-settings
      - python-jose[cryptography]
      - passlib[bcrypt]
      - python-multipart
      - httpx
      - pytest
      - pytest-asyncio
      - python-dotenv
      - pillow
```

这里的核心依赖对应文档要求：

| 依赖                | 作用               |
| ------------------- | ------------------ |
| `fastapi`           | 后端 API 框架      |
| `uvicorn`           | 启动 FastAPI 服务  |
| `sqlalchemy`        | ORM 数据库操作     |
| `alembic`           | 数据库迁移         |
| `pymysql`           | 连接 MySQL         |
| `pydantic-settings` | 读取环境变量配置   |
| `python-jose`       | JWT 生成与校验     |
| `python-multipart`  | 文件上传           |
| `httpx`             | 调用短信或 AI 接口 |
| `pytest`            | 后端自动化测试     |
| `pillow`            | 图片基础处理       |

------

## 2. 创建 Conda 环境

在项目根目录执行：

```
conda env create -f environment.yml
```

创建完成后激活环境：

```
conda activate take-medicine-api
```

检查 Python 版本：

```
python --version
```

建议看到类似：

```
Python 3.11.x
```

检查 FastAPI 是否安装成功：

```
python -c "import fastapi; print('fastapi ok')"
```

------

## 3. 后续依赖变化怎么同步

如果后面新增了依赖，例如短信 SDK、阿里云百炼 SDK，就要修改 `environment.yml`。

修改后，其他成员同步环境用：

```
conda env update -f environment.yml --prune
```

注意：
 不要让每个人自己随便 `pip install`，否则两个人环境会不一致。

------

# 三、创建 MySQL Docker 数据库环境

## 1. 创建 `docker-compose.yml`

在项目根目录创建：

```
docker-compose.yml
```

内容可以这样写：

```
services:
  mysql:
    image: mysql:8.0
    container_name: take_medicine_mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root123456
      MYSQL_DATABASE: take_medicine
      MYSQL_USER: take_user
      MYSQL_PASSWORD: take_pass123
      TZ: Asia/Shanghai
    ports:
      - "3306:3306"
    command:
      [
        "--character-set-server=utf8mb4",
        "--collation-server=utf8mb4_unicode_ci",
        "--default-time-zone=+08:00"
      ]
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
```

文档中要求数据库使用 MySQL 8.0，并固定端口、字符集 `utf8mb4`、时区 `Asia/Shanghai`，同时使用数据卷持久化。

------

## 2. 启动 MySQL

在项目根目录执行：

```
docker compose up -d mysql
```

查看容器是否启动：

```
docker ps
```

看到类似：

```
take_medicine_mysql
```

说明 MySQL 启动成功。

------

## 3. 测试连接 MySQL

可以执行：

```
docker exec -it take_medicine_mysql mysql -u take_user -p
```

输入密码：

```
take_pass123
```

进入 MySQL 后执行：

```
SHOW DATABASES;
```

应该能看到：

```
take_medicine
```

------

# 四、创建环境变量文件

## 1. 创建 `.env.example`

在项目根目录创建：

```
.env.example
```

内容可以这样写：

```
# App
APP_NAME=Take Medicine API
APP_ENV=development
DEBUG=true

# Server
HOST=0.0.0.0
PORT=8000

# Database
DATABASE_URL=mysql+pymysql://take_user:take_pass123@127.0.0.1:3306/take_medicine?charset=utf8mb4

# JWT
JWT_SECRET_KEY=please-change-this-secret
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=10080

# SMS
SMS_MODE=mock
SMS_TEST_CODE=123456
ALIYUN_SMS_ACCESS_KEY_ID=
ALIYUN_SMS_ACCESS_KEY_SECRET=
ALIYUN_SMS_SIGN_NAME=
ALIYUN_SMS_TEMPLATE_CODE=

# AI Recognition
AI_RECOGNITION_MODE=mock
BAILIAN_API_KEY=
BAILIAN_MODEL=qwen-vl-plus

# Upload
UPLOAD_DIR=uploads
MAX_UPLOAD_SIZE_MB=10
```

------

## 2. 本地创建 `.env`

每个人在自己电脑上复制一份：

```
cp .env.example .env
```

Windows PowerShell 可以用：

```
Copy-Item .env.example .env
```

注意：

```
.env.example 要提交到 Git
.env 不要提交到 Git
```

因为 `.env` 里面以后会放真实数据库密码、JWT 密钥、短信密钥、AI API Key。

------

# 五、创建后端 FastAPI 基础工程

进入后端目录：

```
mkdir backend
cd backend
```

建议结构：

```
backend/
├── app/
│   ├── main.py
│   ├── core/
│   │   ├── config.py
│   │   └── database.py
│   ├── api/
│   ├── models/
│   ├── schemas/
│   └── services/
└── tests/
```

创建目录：

```
mkdir app
mkdir app/core app/api app/models app/schemas app/services tests
```

Windows PowerShell 可以用：

```
mkdir app
mkdir app/core
mkdir app/api
mkdir app/models
mkdir app/schemas
mkdir app/services
mkdir tests
```

------

## 1. 创建 `backend/app/main.py`

```
from fastapi import FastAPI

app = FastAPI(
    title="按时吃药 API",
    version="0.1.0"
)

@app.get("/health")
def health_check():
    return {
        "status": "ok",
        "message": "Take Medicine API is running"
    }
```

------

## 2. 启动后端

确保你已经激活 conda 环境：

```
conda activate take-medicine-api
```

然后在 `backend` 目录下执行：

```
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

浏览器访问：

```
http://127.0.0.1:8000/health
```

如果返回：

```
{
  "status": "ok",
  "message": "Take Medicine API is running"
}
```

说明后端基础环境创建成功。

Swagger 文档地址：

```
http://127.0.0.1:8000/docs
```

------

# 六、初始化 Alembic 数据库迁移

在 `backend` 目录执行：

```
alembic init alembic
```

生成结构：

```
backend/
├── alembic/
├── alembic.ini
└── app/
```

后面数据库模型写好后，用：

```
alembic revision --autogenerate -m "init tables"
alembic upgrade head
```

------

# 七、创建前端环境

文档中前端不强制 Docker 化，使用本机：

```
HBuilderX
Node.js
uni-app
Android 调试环境
```

前端需要保证这些文件提交：

```
package.json
package-lock.json 或 pnpm-lock.yaml
前端环境变量配置文件
```

建议创建：

```
mkdir frontend
```

然后用 HBuilderX 创建 uni-app + Vue 3 项目，位置选择：

```
Take_medicineAPP/frontend
```

前端 API 地址建议不要写死，要放到配置文件里，例如：

```
export const BASE_URL = "http://192.168.x.x:8000"
```

真机调试时注意：
 Android 手机不能访问电脑的 `127.0.0.1`，要写电脑局域网 IP。

------

# 八、Git 需要提交和不能提交的文件

## 应该提交

```
environment.yml
docker-compose.yml
.env.example
backend/
frontend/
docs/
alembic 迁移文件
package.json
package-lock.json 或 pnpm-lock.yaml
```

## 不应该提交

```
.env
真实短信密钥
真实 AI API Key
生产数据库密码
本地上传图片
本地数据库数据卷
node_modules/
__pycache__/
uploads/
dist/
unpackage/
```

建议创建 `.gitignore`：

```
# env
.env

# Python
__pycache__/
*.pyc
.pytest_cache/

# Uploads
uploads/

# Node
node_modules/
dist/
unpackage/

# Database
mysql_data/

# IDE
.idea/
.vscode/

# OS
.DS_Store
Thumbs.db
```

------

# 九、完整创建顺序总结

你可以按照这个顺序创建环境：

```
1. 创建项目目录 Take_medicineAPP
2. 创建 environment.yml
3. 执行 conda env create -f environment.yml
4. 激活 conda activate take-medicine-api
5. 创建 docker-compose.yml
6. 执行 docker compose up -d mysql
7. 创建 .env.example
8. 复制 .env.example 为 .env
9. 创建 backend FastAPI 项目结构
10. 创建 /health 接口
11. 启动 uvicorn 测试后端
12. 初始化 Alembic
13. 创建 frontend uni-app 项目
14. 配置前端 API 地址
15. 创建 .gitignore
16. 提交 environment.yml、docker-compose.yml、.env.example 和项目骨架
```

------

# 十、最常用命令汇总

## 创建后端环境

```
conda env create -f environment.yml
conda activate take-medicine-api
```

## 更新后端环境

```
conda env update -f environment.yml --prune
```

## 启动 MySQL

```
docker compose up -d mysql
```

## 查看 MySQL 容器

```
docker ps
```

## 启动后端

```
cd backend
conda activate take-medicine-api
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

## 初始化 Alembic

```
cd backend
alembic init alembic
```

## 创建迁移

```
alembic revision --autogenerate -m "init tables"
```

## 执行迁移

```
alembic upgrade head
```

------

# 十一、一句话总结

这个文档里的环境创建主要是：

```
后端：用 environment.yml 创建 conda 环境 take-medicine-api
数据库：用 docker-compose.yml 启动 MySQL 8.0
前端：用 HBuilderX + Node.js + uni-app 创建 Android 项目
配置：用 .env.example 提供模板，本地复制成 .env
```

核心命令是：

```
conda env create -f environment.yml
conda activate take-medicine-api
docker compose up -d mysql
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```