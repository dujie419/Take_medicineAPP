# 按时吃药 App 代码编写计划书

## 1. 文档目标

本文档根据《项目功能需求说明书-按时吃药App.md》编写，用于指导“按时吃药”Android App 的代码实现。

项目需严格满足需求说明书中的功能、技术栈、安全、测试和交付要求。后端开发环境统一使用项目专属 conda 环境，环境名称约定为：

```bash
take-medicine-api
```

该环境不依赖成员个人已有环境，需通过仓库中的 `environment.yml` 创建：

```bash
conda env create -f environment.yml
conda activate take-medicine-api
```

数据库环境建议使用 Docker Compose 统一 MySQL 版本、端口、字符集和时区。

## 2. 总体技术方案

### 2.1 项目结构

建议项目从零搭建，采用前后端分离结构：

```text
Take_medicineAPP/
  backend/       FastAPI 后端服务
  frontend/      uni-app + Vue 3 Android 前端
  docs/          项目文档、接口说明、ER 图、测试报告
  environment.yml
  docker-compose.yml
  .env.example
```

### 2.2 技术栈

| 模块 | 技术 |
| --- | --- |
| Android 前端 | uni-app + Vue 3 |
| Android 运行环境 | App-Plus / APK |
| 后端 API | FastAPI |
| 后端运行环境 | conda take-medicine-api |
| 后端环境定义 | environment.yml |
| 数据库 | Docker Compose + MySQL 8.0 |
| ORM | SQLAlchemy |
| 数据库迁移 | Alembic |
| 身份认证 | JWT |
| 短信验证码 | 开发固定测试码 + 阿里云短信生产适配 |
| AI 图片识药 | 阿里云百炼视觉模型 |
| 自动化测试 | pytest |

### 2.3 核心目标

1. 实现手机号验证码登录和首次自动注册。
2. 实现拍照或相册上传图片识药。
3. AI 识别结果必须经过人工确认后才能保存。
4. 用户可以管理药品和用药计划。
5. “今天”页面展示当天提醒，并支持同一时间多药合并。
6. Android 到点产生本地通知、震动和中文语音提醒。
7. 用户可以标记已服用，并生成服药记录。
8. 所有用户数据严格隔离。
9. 后端具备基础自动化测试。
10. 项目附带完整运行说明、环境变量示例和数据库初始化方式。

## 3. 实施顺序

1. 创建 `environment.yml`，统一后端 conda 环境 `take-medicine-api`。
2. 创建 `docker-compose.yml`，统一 MySQL 8.0 数据库环境。
3. 初始化后端 FastAPI 工程，并确认 `take-medicine-api` 环境可运行。
4. 完成后端配置、数据库连接、SQLAlchemy 基础模型和 Alembic 迁移。
5. 实现用户认证、JWT 鉴权和用户资料接口。
6. 实现药品、计划、今日提醒、服药记录等核心业务接口。
7. 接入开发固定验证码和阿里云短信生产适配。
8. 接入阿里云百炼视觉识药，并提供测试 mock。
9. 初始化 uni-app + Vue 3 前端工程。
10. 完成登录、底部导航、今天、识药、计划、我的页面。
11. 完成 Android 本地通知、震动、TTS、录音和权限处理。
12. 补齐后端自动化测试、Android 真机测试、运行文档和交付材料。

## 4. 模块划分与设计目标

## 4.1 项目基础模块

### 包含内容

- 后端 FastAPI 项目结构。
- 前端 uni-app + Vue 3 项目结构。
- 环境变量配置。
- MySQL 数据库连接。
- Alembic 数据库迁移。
- 项目统一 conda 环境 `take-medicine-api` 运行配置。
- `environment.yml` 后端依赖定义。
- `docker-compose.yml` MySQL 数据库环境定义。
- `.env.example` 环境变量模板。
- 开发、测试、生产配置区分。

### 需要达到的效果

- 后端可通过 `conda activate take-medicine-api` 后正常启动。
- 两名成员都能通过同一份 `environment.yml` 创建一致的 Python 环境。
- MySQL 可通过 Docker Compose 启动，并保持版本、端口、字符集和时区一致。
- FastAPI 能访问 Docker Compose 中的 MySQL。
- `/health` 健康检查接口可用。
- 前端可通过环境变量切换 API 地址。
- 项目目录结构清晰，方便后续模块开发。

## 4.2 用户认证与权限模块

### 包含内容

- 手机号验证码发送。
- 验证码摘要存储。
- 验证码过期、限流、错误次数限制。
- 手机号验证码登录。
- 首次登录自动注册。
- JWT 生成与校验。
- 当前用户信息获取与修改。
- 用户停用校验。

### 需要达到的效果

- 用户输入手机号和验证码即可登录。
- 首次登录自动创建账号。
- 登录后前端保存 JWT 和用户信息。
- 所有受保护接口必须校验 JWT。
- Token 无效或过期时返回 401。
- 用户被停用时返回 403。
- 用户只能访问自己的数据。
- 开发环境可使用固定测试验证码，生产环境使用阿里云短信。

## 4.3 药品管理模块

### 包含内容

- 新增药品。
- 查询药品列表。
- 查询药品详情。
- 药品与用户绑定。
- 药品基础信息存储。
- 药品原始图片路径记录。
- AI 识别可信度记录。

### 需要达到的效果

- 用户可以保存自己的药品信息。
- 药品列表按创建时间倒序展示。
- 用户不能查看或操作其他用户的药品。
- 药品名称不能为空。
- 药品信息可被计划模块选择使用。

## 4.4 AI 图片识药模块

### 包含内容

- 图片上传接口。
- 图片类型校验：JPEG、PNG、WebP。
- 图片大小限制，例如 10 MB。
- 图片随机命名。
- 图片按用户目录隔离存储。
- 调用阿里云百炼视觉模型。
- 解析结构化 JSON。
- 保存 AI 识别记录。
- 识别失败、超时、格式异常处理。

### 需要达到的效果

- 用户可以拍照或从相册选择图片上传识别。
- 后端返回结构化识药结果，包括药名、规格、服用量、每日次数、提醒时间、说明、可信度、风险提示。
- AI 结果不能直接入库，必须由用户人工确认。
- 无法可靠识别的内容必须提示用户核对。
- 每次识别都记录图片、模型、结构化结果、原始响应和错误信息。
- 系统不得给出诊断、处方调整或停药建议。

## 4.5 用药计划模块

### 包含内容

- 新增用药计划。
- 查询计划列表。
- 查询计划详情。
- 修改计划。
- 删除计划。
- 启用或暂停计划。
- 提醒时间管理。
- 开始日期、结束日期校验。

### 需要达到的效果

- 用户可以为已有药品创建服药计划。
- 每日次数必须和提醒时间数量一致。
- 同一计划内提醒时间不能重复。
- 结束日期不能早于开始日期。
- 暂停计划后不再生成今日提醒。
- 删除计划后，本地通知也要刷新，不再提醒该药品。

## 4.6 今日提醒模块

### 包含内容

- 根据用户时区计算今天日期。
- 查询当前用户有效计划。
- 按提醒时间生成当天提醒。
- 同一时间多个药品合并。
- 生成中文语音播报文案。
- 判断提醒完成状态。

### 需要达到的效果

- “今天”页面能展示当天所有提醒。
- 同一时间服用的多个药品合并成一张提醒卡片。
- 每张卡片展示时间、药品、服用量和状态。
- 返回中文播报文本，例如：

```text
张阿姨，现在上午八点了，该吃阿司匹林、二甲双胍了。
```

- 已服用的提醒不再重复提醒。
- 今日提醒按时间升序排列。

## 4.7 服药记录模块

### 包含内容

- 创建服药记录。
- 更新服药记录。
- 查询历史服药记录。
- 支持状态：`taken`、`skipped`、`snoozed`。
- 服药记录幂等处理。
- 稍后提醒时间处理。

### 需要达到的效果

- 用户点击“已服用”后，为该提醒组内每个计划分别生成记录。
- 重复点击不会产生重复记录。
- 同一计划、同一计划服药时间只能有一条记录。
- 历史记录按计划服药时间倒序展示。
- 后续可扩展“跳过本次”和“稍后提醒”。

## 4.8 前端登录与导航模块

### 包含内容

- 登录页。
- 手机号输入。
- 验证码输入。
- 获取验证码倒计时。
- 登录按钮状态控制。
- 底部四栏导航：今天、识药、计划、我的。
- JWT 本地持久化。
- 401 自动退出。

### 需要达到的效果

- 登录流程简单，适合老年用户。
- 按钮字号和点击区域偏大。
- 登录成功后进入“今天”页面。
- 再次打开 App 时自动恢复登录状态。
- Token 失效时清除本地信息并返回登录页。
- 页面切换不会产生重复页面堆栈。

## 4.9 前端“今天”页面模块

### 包含内容

- 问候语。
- 用户称呼展示与修改。
- 今日日期。
- 今日提醒统计。
- 完成进度。
- 语音提醒状态。
- 提醒卡片列表。
- 已服用二次确认。
- 下拉刷新。

### 需要达到的效果

- 用户一打开 App 就能看到今天该吃什么药。
- 同一时间多药合并展示。
- 点击“已服用”前弹窗确认，并列出本组药品。
- 确认后立即更新状态。
- 修改称呼后，提醒语音文案同步更新。

## 4.10 前端“识药”页面模块

### 包含内容

- 拍照。
- 相册选择。
- 图片压缩。
- 图片预览。
- 上传识别。
- AI 结果展示。
- 药品信息人工编辑。
- 删除误识别药品。
- 添加漏识别药品。
- 批量保存药品和计划。

### 需要达到的效果

- 用户可以通过拍照或相册完成识药。
- 识别期间有明确加载状态，防止重复提交。
- AI 识别结果全部可编辑。
- 药品名称、每日次数、提醒时间必须校验。
- 保存后同时创建药品和对应计划。
- 保存完成后刷新今日提醒和本地通知。

## 4.11 前端“计划”页面模块

### 包含内容

- 计划列表。
- 新增计划。
- 编辑计划。
- 删除计划。
- 暂停或启用计划。
- 空状态引导。
- 下拉刷新。

### 需要达到的效果

- 用户可以查看全部用药计划。
- 每个计划显示药品名、服用量、每日次数、提醒时间、开始日期和状态。
- 删除前必须二次确认。
- 修改、删除、暂停计划后，Android 本地通知同步刷新。

## 4.12 前端“我的”页面模块

### 包含内容

- 用户称呼。
- 手机号。
- 时区。
- 药品数量。
- 服药记录数量。
- 我的药品列表。
- 最近服药记录。
- 退出登录。

### 需要达到的效果

- 用户可以查看自己的基础信息和数据统计。
- 可以看到已保存药品和最近服药记录。
- 退出登录前二次确认。
- 退出后清除 JWT、用户信息、本地通知和提醒轮询。

## 4.13 Android 本地提醒模块

### 包含内容

- Android 通知权限。
- 本地通知调度。
- 通知点击跳转。
- 震动。
- 重复提醒。
- 中文 TTS。
- 自定义录音提醒。
- 前后台提醒策略。

### 需要达到的效果

- App 同步今日提醒后，能在 Android 本地设置通知。
- 到点后产生系统通知、震动和中文语音。
- 用户未确认服药时，每 5 分钟重复提醒，最长 2 小时。
- 已确认服药后停止重复提醒。
- 录制的自定义提醒音按“用户 + 提醒组”隔离。
- 退出账号后不能继续提醒。

## 4.14 异常处理与安全模块

### 包含内容

- 网络异常。
- 请求超时。
- 参数校验失败。
- 登录过期。
- 权限不足。
- 数据不存在。
- 验证码错误。
- 图片格式错误。
- AI 服务异常。
- 第三方短信异常。
- 密钥和隐私保护。

### 需要达到的效果

- 前端所有错误提示使用用户能理解的中文。
- 不展示后端堆栈、数据库异常或密钥。
- 生产环境不输出验证码、Token、短信密钥、AI Key。
- 上传文件不能路径注入。
- CORS 生产环境不能无条件放开。
- 所有资源操作都必须校验当前用户归属。

## 4.15 测试模块

### 包含内容

- 后端 pytest 自动化测试。
- 前端基础交互测试。
- Android 真机测试。
- API 联调测试。
- 第三方服务 mock 测试。

### 需要达到的效果

- 后端至少覆盖需求文档中的核心测试项。
- 验证用户数据隔离。
- 验证同一时间多药合并。
- 验证服药记录幂等。
- 验证 AI 返回结构校验和异常处理。
- 真机验证通知、TTS、震动、录音、权限拒绝、前后台、退出登录。

## 4.16 文档与交付模块

### 包含内容

- 项目运行说明。
- `.env.example`。
- 数据库初始化说明。
- API 文档。
- 测试报告。
- ER 图。
- APK 构建说明。
- 已知问题和后续改进说明。

### 需要达到的效果

- 其他人能按照文档在本机启动项目。
- 后端能在 `take-medicine-api` 环境中运行。
- 数据库迁移和初始化步骤清楚。
- 前后端联调地址配置清楚。
- 提交内容不包含真实短信密钥、AI Key、数据库密码或隐私数据。

## 5. 团队环境统一方案

### 5.1 conda 后端环境

后端不使用成员个人已有环境，统一通过 `environment.yml` 创建：

```bash
conda env create -f environment.yml
conda activate take-medicine-api
```

`environment.yml` 至少应固定：

- 环境名：`take-medicine-api`。
- Python 版本：建议 `3.11`。
- FastAPI、Uvicorn、SQLAlchemy、Alembic、PyMySQL。
- Pydantic Settings、JWT、文件上传、HTTP 客户端、pytest 等依赖。

当后端依赖变化时，应修改并提交 `environment.yml`，其他成员通过以下命令同步：

```bash
conda env update -f environment.yml --prune
```

### 5.2 Docker 数据库环境

数据库使用 `docker-compose.yml` 统一 MySQL 环境。

建议固定：

- MySQL 版本：`mysql:8.0`。
- 默认数据库名。
- 开发用户名和密码。
- 本机映射端口。
- 字符集：`utf8mb4`。
- 时区：`Asia/Shanghai`。
- 数据卷持久化。

启动数据库：

```bash
docker compose up -d mysql
```

### 5.3 前端环境

前端不强制 Docker 化，使用本机 HBuilderX、Node.js 和 Android 调试环境。

前端需要通过以下文件保证依赖一致：

- `package.json`
- `package-lock.json` 或 `pnpm-lock.yaml`
- 前端环境变量配置文件

### 5.4 配置文件提交规则

应提交：

- `environment.yml`
- `docker-compose.yml`
- `.env.example`
- 前端依赖锁文件
- 后端迁移文件

不得提交：

- `.env`
- 真实短信密钥
- 真实 AI API Key
- 生产数据库密码
- 本地上传图片
- 本地数据库数据卷

## 6. 数据库设计计划

至少创建以下数据表：

| 表名 | 作用 |
| --- | --- |
| `users` | 用户信息 |
| `sms_verification_codes` | 短信验证码记录 |
| `medicines` | 用户药品 |
| `medication_plans` | 用药计划 |
| `reminder_times` | 计划提醒时间 |
| `medication_logs` | 服药记录 |
| `ai_recognition_records` | AI 识药记录 |

关键约束：

- 用户手机号唯一。
- 同一计划的同一提醒时间唯一。
- 同一计划与同一计划服药时间的日志唯一。
- 主要外键建立索引。
- 所有主要业务表记录创建时间和更新时间。
- 用户删除后，其药品和计划按设计级联处理。
- 计划删除后，提醒时间和服药记录保持一致。

## 7. API 设计计划

建议实现以下 RESTful API：

| 方法 | 路径 | 功能 | 是否登录 |
| --- | --- | --- | --- |
| GET | `/health` | 服务健康检查 | 否 |
| POST | `/api/v1/auth/sms-codes` | 获取短信验证码 | 否 |
| POST | `/api/v1/auth/login` | 验证码登录和自动注册 | 否 |
| GET | `/api/v1/auth/me` | 获取当前用户 | 是 |
| PATCH | `/api/v1/auth/me` | 修改用户称呼 | 是 |
| POST | `/api/v1/medicines/recognize` | 上传图片并识别药品 | 是 |
| GET | `/api/v1/medicines` | 查询药品列表 | 是 |
| POST | `/api/v1/medicines` | 新增药品 | 是 |
| GET | `/api/v1/medicines/{id}` | 查询药品详情 | 是 |
| GET | `/api/v1/plans` | 查询用药计划 | 是 |
| POST | `/api/v1/plans` | 新增用药计划 | 是 |
| GET | `/api/v1/plans/{id}` | 查询计划详情 | 是 |
| PUT | `/api/v1/plans/{id}` | 修改用药计划 | 是 |
| DELETE | `/api/v1/plans/{id}` | 删除用药计划 | 是 |
| GET | `/api/v1/today` | 查询当天组合提醒 | 是 |
| GET | `/api/v1/medication-logs` | 查询服药记录 | 是 |
| POST | `/api/v1/medication-logs` | 创建或更新服药记录 | 是 |

## 8. 测试与验收计划

### 8.1 后端自动化测试

至少覆盖：

- 健康检查。
- CORS。
- 手机号格式校验。
- 验证码登录。
- JWT 鉴权。
- 用户数据隔离。
- 药品新增和查询。
- 计划新增、修改、删除。
- 重复提醒时间校验。
- 结束日期校验。
- 今日提醒生成。
- 同一时间多药合并。
- 服药记录幂等。
- 用户时区计算。
- AI 返回结构校验。
- AI 服务异常处理。

### 8.2 Android 真机测试

至少验证：

- 首次安装权限流程。
- 手机号登录。
- 登录状态持久化。
- 相机拍照。
- 相册选图。
- 图片上传。
- AI 识别结果展示。
- 药品和计划保存。
- 到点通知。
- 通知点击跳转。
- 中文语音。
- 震动。
- 自定义录音。
- 前后台切换。
- 断网和弱网。
- Token 失效。
- 修改、暂停和删除计划后通知是否正确更新。
- 退出账号后是否仍会错误提醒。

## 9. 交付物

项目完成后应提交：

- 前端源代码。
- 后端源代码。
- 数据库迁移文件。
- 环境变量示例文件。
- API 文档。
- 项目运行说明。
- 测试报告。
- Android APK。
- 演示视频。
- 数据库设计图或 ER 图。
- 项目分工说明。
- 已知问题和后续改进说明。

不得提交：

- 真实短信密钥。
- 真实 AI API Key。
- 生产数据库密码。
- 用户隐私数据。
- 包含有效 Token 的日志。

## 10. 首版范围说明

首版优先完成需求说明书中的基础验收标准。

以下能力可作为后续提高功能，不阻塞首版验收：

- 家属账号与老人账号绑定。
- 家属远程查看服药状态。
- 日历视图和周/月统计。
- 药品库存和余量提醒。
- 药品有效期提醒。
- 离线缓存和断网补传。
- HTTPS 和域名部署。
- Docker 一键部署。
- 开机后自动恢复提醒。
- Android 电池优化引导。
