# 一、推荐总分工方式

建议两个人按 **业务闭环** 分：

| 成员   | 负责业务线                                        | 核心目标                                 |
| ------ | ------------------------------------------------- | ---------------------------------------- |
| A 同学 | 账号登录 + 用户资料 + 药品管理 + AI 识药          | 用户能登录、拍照识药、人工确认、保存药品 |
| B 同学 | 用药计划 + 今日提醒 + Android 通知语音 + 服药记录 | 用户能设置计划、收到提醒、标记已服用     |

这种分工的好处是：
 每个人负责的不是单个页面或单个接口，而是一条完整业务链。谁负责某个业务模块，就同时负责这个模块相关的页面、接口、数据库、测试、文档。

------

# 二、A 同学详细分工：账号与识药业务线

A 同学负责从 **用户进入 App 到识别并保存药品** 的完整流程。

## A-1. 项目基础协作任务

虽然不按前后端分离，但项目基础必须先统一。A 可以主导这部分，B 配合确认环境。

A 负责：

- 创建后端 conda 环境文件 `environment.yml`
- 约定环境名：`take-medicine-api`
- 创建 `.env.example`
- 配置 FastAPI 基础工程
- 配置 MySQL 连接
- 配置 SQLAlchemy
- 配置 Alembic
- 配置 `/health` 健康检查接口
- 配置统一错误返回格式

代码编写计划书明确要求后端统一使用 `take-medicine-api` 环境，并通过 `environment.yml` 创建；数据库建议用 Docker Compose 统一 MySQL 版本、端口、字符集和时区。

A 需要交付：

```
backend/
  app/
    main.py
    core/
      config.py
      database.py
      security.py
    models/
    schemas/
    api/
    services/
  alembic/
environment.yml
.env.example
```

注意事项：

- 不要使用个人电脑里已有的 Python 环境。
- 后端依赖变化后，要更新 `environment.yml`。
- `.env` 不能提交，只提交 `.env.example`。
- MySQL 字符集必须用 `utf8mb4`，避免中文和特殊字符存储问题。
- 数据库时区建议统一为 `Asia/Shanghai`。

------

## A-2. 用户认证与权限模块

A 负责完整登录闭环：

### 页面部分

- 登录页
- 手机号输入
- 验证码输入
- 获取验证码按钮
- 倒计时
- 登录按钮加载状态
- 登录错误提示
- 登录成功后保存 JWT
- Token 失效后返回登录页

### 接口部分

A 负责这些接口：

```
POST /api/v1/auth/sms-codes
POST /api/v1/auth/login
GET /api/v1/auth/me
PATCH /api/v1/auth/me
```

### 数据表

A 负责：

```
users
sms_verification_codes
```

### 需要实现的效果

- 用户输入手机号和验证码即可登录。
- 首次登录自动创建账号。
- 登录成功返回 JWT 和用户信息。
- 所有受保护接口校验 JWT。
- Token 无效或过期返回 401。
- 用户被停用返回 403。
- 用户只能访问自己的数据。

需求文档中明确要求手机号验证码登录，首次登录自动创建账号，并且所有需要登录的接口都必须验证 JWT。

### A 的注意事项

第一，手机号格式要前后端都校验：

```
11 位
以 1 开头
第二位为 3～9
```

第二，验证码不能明文存数据库。建议保存验证码摘要。

第三，开发环境可以使用固定测试验证码，例如：

```
测试验证码：123456
```

但生产环境不能返回验证码，也不能把验证码打印到日志里。

第四，JWT 密钥不能硬编码，要写在 `.env` 中。

第五，所有需要登录的接口统一使用：

```
Authorization: Bearer <token>
```

------

## A-3. 用户资料模块

A 负责用户资料相关功能。

### 页面部分

- “我的”页面中的用户称呼
- 手机号展示
- 时区展示
- 修改称呼
- 登录状态展示

### 接口部分

```
GET /api/v1/auth/me
PATCH /api/v1/auth/me
```

### 需要实现的效果

- 用户可以查看自己的称呼、手机号、时区。
- 用户可以修改称呼。
- 称呼不能为空。
- 修改称呼后，本地用户信息同步更新。
- 今日提醒语音中的称呼也要同步更新。

### 注意事项

修改称呼不是单独的“小功能”，它会影响 B 同学负责的今日提醒语音。例如播报：

```
张阿姨，现在上午八点了，该吃阿司匹林了。
```

所以 A 修改称呼成功后，要保证 B 的今日提醒模块能拿到最新称呼。

------

## A-4. 药品管理模块

A 负责药品从保存到查询的完整闭环。

### 页面部分

- 我的药品列表
- 药品新增表单
- 药品详情展示
- 识药确认后保存药品

### 接口部分

```
GET /api/v1/medicines
POST /api/v1/medicines
GET /api/v1/medicines/{id}
```

### 数据表

```
medicines
```

### 药品字段建议

```
id
user_id
name
specification
dosage
usage_note
original_image_path
ai_confidence
created_at
updated_at
```

### 需要实现的效果

- 用户可以保存自己的药品。
- 药品列表按创建时间倒序展示。
- 用户不能查看或操作其他用户的药品。
- 药品名称不能为空。
- 药品可以被 B 同学的计划模块选择使用。

需求文档中药品字段至少包括 ID、用户 ID、名称、规格、每次服用量、说明、图片路径、AI 识别可信度、创建时间和更新时间。

### 注意事项

所有药品查询都必须带当前用户过滤条件。

错误写法：

```
select(Medicine).where(Medicine.id == medicine_id)
```

正确写法：

```
select(Medicine).where(
    Medicine.id == medicine_id,
    Medicine.user_id == current_user.id
)
```

------

## A-5. AI 图片识药模块

这是 A 同学最核心的功能。

### 页面部分

- 识药页面
- 拍照
- 相册选择
- 图片预览
- 图片压缩
- 上传识别
- 识别中 loading
- AI 结果展示
- 药品信息编辑
- 删除误识别药品
- 添加漏识别药品
- 批量保存药品

### 接口部分

```
POST /api/v1/medicines/recognize
POST /api/v1/medicines
```

如果识别后同时创建计划，需要和 B 同学约定接口：

```
POST /api/v1/plans
```

### 数据表

```
ai_recognition_records
medicines
```

### AI 识别返回字段

每种药品建议返回：

```
{
  "name": "阿司匹林",
  "specification": "100mg",
  "dosage": "1片",
  "daily_times": 1,
  "suggested_times": ["08:00"],
  "usage_note": "饭后服用",
  "confidence": 0.86,
  "risk_notice": "请核对药盒或医生处方",
  "need_confirm": true
}
```

整张图片建议返回：

```
{
  "document_type": "药盒",
  "medicines": [],
  "overall_risk_notice": "AI识别结果仅供参考，请人工确认"
}
```

### 需要实现的效果

- 支持拍照或相册上传。
- 支持识别药盒、药瓶、处方、用药清单。
- 后端调用阿里云百炼视觉模型。
- 返回结构化 JSON。
- AI 结果必须展示给用户确认。
- 用户确认后才能保存药品。
- 每次识别都保存识别记录。

代码计划书明确 AI 图片识药模块需要图片上传、格式校验、随机命名、按用户目录隔离、调用阿里云百炼视觉模型、解析结构化 JSON、保存识别记录，并处理失败、超时和格式异常。

### A 的重点注意事项

第一，图片上传必须限制：

```
JPEG
PNG
WebP
最大 10MB
```

第二，文件名必须随机生成，不能直接使用用户上传文件名，避免路径注入。

第三，图片要按用户目录隔离，例如：

```
uploads/user_1/recognition/xxx.jpg
uploads/user_2/recognition/yyy.jpg
```

第四，AI 结果不能直接入库。必须：

```
AI 识别 → 用户编辑确认 → 保存药品 → 创建计划
```

第五，系统不能给出诊断、处方调整、停药建议。需求文档明确说明本项目是用药信息管理和提醒工具，不是医疗诊断或处方系统。

------

# 三、B 同学详细分工：计划与提醒业务线

B 同学负责从 **创建用药计划到提醒、确认、记录** 的完整流程。

## B-1. 用药计划模块

B 负责计划的完整闭环。

### 页面部分

- 计划列表页面
- 新增计划页面
- 编辑计划页面
- 删除计划确认弹窗
- 暂停 / 启用计划
- 空状态引导用户去识药或添加药品

### 接口部分

```
GET /api/v1/plans
POST /api/v1/plans
GET /api/v1/plans/{id}
PUT /api/v1/plans/{id}
DELETE /api/v1/plans/{id}
```

### 数据表

```
medication_plans
reminder_times
```

### 计划字段建议

```
id
user_id
medicine_id
daily_times
start_date
end_date
remark
is_enabled
created_at
updated_at
```

### 提醒时间字段建议

```
id
plan_id
reminder_time
created_at
updated_at
```

### 需要实现的效果

- 用户可以为已有药品创建计划。
- 用户可以查看计划列表。
- 用户可以修改计划。
- 用户可以删除计划。
- 用户可以暂停或启用计划。
- 删除前必须二次确认。
- 暂停计划后不再生成今日提醒。

需求文档要求计划支持创建、查询、修改、删除，并校验每日次数、提醒时间数量、开始日期、结束日期和用户归属。

### B 的重点注意事项

第一，每日次数必须等于提醒时间数量。

正确：

```
{
  "daily_times": 3,
  "reminder_times": ["08:00", "12:00", "19:00"]
}
```

错误：

```
{
  "daily_times": 3,
  "reminder_times": ["08:00", "19:00"]
}
```

第二，同一计划内提醒时间不能重复。

错误：

```
{
  "daily_times": 2,
  "reminder_times": ["08:00", "08:00"]
}
```

第三，结束日期不能早于开始日期。

第四，编辑已有计划时，药品不建议随意更换。如果要换药，建议删除计划后重新创建。

第五，修改、暂停、删除计划后，必须通知 Android 本地提醒模块重新刷新通知。

------

## B-2. 今日提醒模块

B 负责“今天”页面和今日提醒接口。

### 页面部分

- 问候语
- 用户称呼展示
- 今日日期
- 今日提醒总数
- 已完成数
- 待完成数
- 完成进度
- 语音提醒状态
- 提醒卡片列表
- 下拉刷新
- 已服用按钮
- 已服用二次确认弹窗

### 接口部分

```
GET /api/v1/today
```

### 需要实现的效果

- 根据用户时区计算今天。
- 查询当前用户有效计划。
- 只展示启用中的计划。
- 开始日期不晚于今天。
- 结束日期为空或不早于今天。
- 同一时间多个药品合并成一组。
- 按提醒时间升序返回。
- 生成中文语音播报文案。
- 判断当前提醒是否已完成。

代码计划书明确今日提醒模块需要根据用户时区计算今天、查询有效计划、同一时间多个药品合并、生成中文语音播报文案并判断完成状态。

### 今日提醒返回结构建议

```
{
  "date": "2026-06-27",
  "summary": {
    "total": 3,
    "done": 1,
    "pending": 2
  },
  "items": [
    {
      "reminder_group_id": "2026-06-27_08:00",
      "time": "08:00",
      "period": "早上",
      "plan_ids": [1, 2],
      "speech_text": "张阿姨，现在上午八点了，该吃阿司匹林、二甲双胍了。",
      "status": "pending",
      "medicines": [
        {
          "medicine_id": 1,
          "plan_id": 1,
          "name": "阿司匹林",
          "dosage": "1片"
        },
        {
          "medicine_id": 2,
          "plan_id": 2,
          "name": "二甲双胍",
          "dosage": "1片"
        }
      ]
    }
  ]
}
```

### B 的重点注意事项

第一，“同一时间多药合并”是验收重点。

例如 08:00 有 3 个计划，不应该返回 3 张卡片，而应该返回 1 个提醒组。

第二，中文播报文案要包含用户称呼和药品名称。

第三，已服用的提醒不能继续重复提醒。

第四，今日提醒必须按用户时区计算，默认 `Asia/Shanghai`。

------

## B-3. 服药记录模块

B 负责用户点击“已服用”之后的记录闭环。

### 页面部分

- 已服用确认弹窗
- 最近服药记录列表
- 历史服药记录页面
- 状态展示：已服用、已跳过、稍后提醒

### 接口部分

```
GET /api/v1/medication-logs
POST /api/v1/medication-logs
```

### 数据表

```
medication_logs
```

### 记录字段建议

```
id
plan_id
planned_at
actual_at
status
snooze_until
remark
created_at
updated_at
```

### 状态

```
taken    已服用
skipped  已跳过
snoozed  稍后提醒
```

### 需要实现的效果

- 用户点击“已服用”后，为提醒组里的每个计划生成记录。
- 同一计划、同一计划服药时间只能有一条记录。
- 重复点击不会生成重复记录。
- 历史记录按计划服药时间倒序展示。
- 后续可以扩展“跳过本次”和“稍后提醒”。

### B 的重点注意事项

服药记录必须做幂等。

建议数据库加唯一约束：

```
plan_id + planned_at 唯一
```

如果用户重复点击“已服用”，应该更新原记录，而不是新增一条。

错误结果：

```
08:00 阿司匹林 已服用
08:00 阿司匹林 已服用
08:00 阿司匹林 已服用
```

正确结果：

```
08:00 阿司匹林 已服用
```

------

## B-4. Android 本地提醒模块

这是 B 同学的重点难点。

### 功能内容

- Android 通知权限
- 本地通知调度
- 到点通知
- 通知点击跳转
- 震动
- 中文 TTS
- 重复提醒
- 自定义录音提醒
- 前后台提醒策略
- 退出登录后清除通知

### 需要实现的效果

- App 同步今日提醒后，设置 Android 本地通知。
- 到点后产生系统通知、震动和中文语音。
- 用户未确认服药时，每 5 分钟重复提醒。
- 重复提醒最长有效窗口为 2 小时。
- 已确认服药后停止重复提醒。
- 自定义录音按“用户 + 提醒组”隔离。
- 退出账号后不能继续提醒。

需求文档明确要求 Android 本地通知、震动、中文语音播报和自定义提醒音，并且退出登录后要清除本地通知。

### B 的重点注意事项

第一，Android 13 及以上需要通知权限。

第二，权限不要一进 App 就全部申请，应该在需要时申请：

| 权限 | 触发时机           |
| ---- | ------------------ |
| 相机 | 用户点击拍照       |
| 相册 | 用户选择图片       |
| 通知 | 第一次设置提醒     |
| 录音 | 用户点击录制提醒音 |
| 震动 | 到点提醒时使用     |

第三，删除、暂停、修改计划后，必须重新安排本地通知。

第四，退出登录后必须清除：

```
JWT
用户信息
本地通知
提醒轮询
自定义提醒状态
```

第五，真机测试非常重要，模拟器不一定能完整验证通知、TTS、震动和录音。

------

# 四、公共模块：两个人共同维护

虽然按业务线分工，但以下内容不能各写各的，必须统一。

## 1. 项目基础结构

计划书建议项目结构为：

```
Take_medicineAPP/
  backend/
  frontend/
  docs/
  environment.yml
  docker-compose.yml
  .env.example
```

虽然你说不要从前后端分离来分工，但代码目录仍然可以保持这个结构。
 区别是：**分工不按目录分，而是按业务线分。**

------

## 2. 环境统一

两个人必须使用同一套环境。

### 后端环境

```
conda env create -f environment.yml
conda activate take-medicine-api
```

### 数据库环境

```
docker compose up -d mysql
```

### 前端环境

- HBuilderX
- Node.js
- uni-app
- Android 真机调试环境

### 注意事项

- A 改了后端依赖，必须更新 `environment.yml`。
- B 改了前端依赖，必须更新 `package.json` 和锁文件。
- `.env` 不提交。
- `.env.example` 必须提交。
- Docker 数据卷不要提交。
- 上传图片目录不要提交。

------

## 3. API 返回格式统一

建议统一格式：

成功：

```
{
  "code": 0,
  "message": "success",
  "data": {}
}
```

失败：

```
{
  "code": 40001,
  "message": "验证码错误或已过期",
  "data": null
}
```

不要出现：

A 的接口返回：

```
{
  "msg": "ok"
}
```

B 的接口返回：

```
{
  "success": true
}
```

这样前端封装会非常混乱。

------

## 4. 时间格式统一

本项目时间非常多，必须提前统一。

建议：

```
{
  "reminder_time": "08:00",
  "start_date": "2026-06-27",
  "end_date": null,
  "planned_at": "2026-06-27T08:00:00+08:00",
  "actual_at": "2026-06-27T08:03:12+08:00"
}
```

注意：

- 每日提醒时间用 `"HH:mm"`。
- 日期用 `"YYYY-MM-DD"`。
- 具体时间点用 ISO 8601。
- 用户时区默认 `Asia/Shanghai`。
- 不要混用 `"8点"`、`"08:00"`、时间戳。

------

## 5. 状态枚举统一

服药记录状态统一：

```
taken
skipped
snoozed
```

计划状态统一：

```
enabled
disabled
```

或者用布尔值：

```
is_enabled: true / false
```

不要一个地方用：

```
已服用
```

另一个地方用：

```
done
```

再另一个地方用：

```
1
```

------

## 6. 数据库命名统一

建议：

| 类型     | 命名方式     |
| -------- | ------------ |
| 表名     | 小写复数     |
| 字段名   | 小写下划线   |
| 主键     | `id`         |
| 外键     | `xxx_id`     |
| 创建时间 | `created_at` |
| 更新时间 | `updated_at` |

例如：

```
users
medicines
medication_plans
reminder_times
medication_logs
ai_recognition_records
```

------

# 五、推荐开发阶段安排

不要两个人各写一堆功能，最后才联调。
 建议按照主流程逐步打通。

------

## 第 1 阶段：项目骨架与环境统一

负责人：A 主导，B 配合

### A 完成

- `environment.yml`
- FastAPI 基础项目
- SQLAlchemy 数据库连接
- Alembic 配置
- `/health`
- `.env.example`

### B 完成

- uni-app 项目初始化
- 底部导航框架
- 请求封装初版
- API 地址环境变量
- Android 真机运行测试

### 共同验收

```
后端能启动
MySQL 能连接
/health 能访问
前端能运行到 Android
前端能请求后端 /health
```

------

## 第 2 阶段：登录闭环

负责人：A

### A 完成

- 用户表
- 验证码表
- 获取验证码接口
- 登录接口
- JWT
- 获取当前用户接口
- 登录页
- Token 保存

### B 配合

- 检查登录成功后跳转“今天”页面
- 检查 401 自动退出
- 检查 Token 传递

### 验收标准

```
用户输入手机号和测试验证码后可以登录
首次登录自动创建用户
重新打开 App 能保持登录
Token 过期能自动退出
```

------

## 第 3 阶段：药品管理闭环

负责人：A

### A 完成

- 药品表
- 药品新增接口
- 药品列表接口
- 药品详情接口
- 我的药品页面
- 药品新增表单

### B 配合

- 在计划页面读取 A 的药品列表
- 用药品数据创建计划

### 验收标准

```
用户可以手动新增药品
用户可以查看自己的药品
用户不能访问别人的药品
计划模块可以选择已有药品
```

------

## 第 4 阶段：计划管理闭环

负责人：B

### B 完成

- 计划表
- 提醒时间表
- 计划新增接口
- 计划列表接口
- 计划详情接口
- 计划修改接口
- 计划删除接口
- 计划页面
- 新增 / 编辑计划页面

### A 配合

- 保证药品接口稳定
- 协助检查用户数据隔离

### 验收标准

```
用户可以选择药品创建计划
每日次数和提醒时间数量一致
提醒时间不能重复
结束日期不能早于开始日期
计划可以修改、暂停、删除
```

------

## 第 5 阶段：今日提醒闭环

负责人：B

### B 完成

- 今日提醒接口
- 同一时间多药合并
- 中文播报文案生成
- 今天页面
- 提醒卡片
- 已服用按钮
- 已服用二次确认

### A 配合

- 提供用户称呼字段
- 保证药品名称和剂量字段准确

### 验收标准

```
今天页面能显示当天提醒
同一时间多个药品合并成一张卡片
提醒按时间升序排列
点击已服用后状态更新
```

------

## 第 6 阶段：服药记录闭环

负责人：B

### B 完成

- 服药记录表
- 创建或更新记录接口
- 历史记录接口
- 幂等处理
- 我的页面最近记录展示

### A 配合

- 我的页面统计数据联调
- 用户归属校验检查

### 验收标准

```
点击已服用能生成记录
重复点击不会生成重复记录
历史记录倒序展示
不同用户记录严格隔离
```

------

## 第 7 阶段：AI 识药闭环

负责人：A

### A 完成

- 图片上传
- 图片类型和大小校验
- 图片随机命名
- 阿里云百炼视觉模型调用
- AI 结果结构化校验
- AI 识别记录表
- 识药页面
- AI 结果人工编辑页面
- 保存识别结果为药品

### B 配合

- 保存药品后创建计划
- 保存完成后刷新今日提醒和本地通知

### 验收标准

```
用户可以拍照或选图
AI 可以返回结构化结果
用户可以修改 AI 结果
确认后能保存药品
可以继续创建计划
AI 结果不会未经确认直接入库
```

------

## 第 8 阶段：Android 本地提醒闭环

负责人：B

### B 完成

- 通知权限申请
- 根据今日提醒设置本地通知
- 到点通知
- 震动
- 中文 TTS
- 重复提醒
- 已服用后停止提醒
- 通知点击跳转
- 退出登录清除通知
- 自定义录音提醒

### A 配合

- 今日提醒接口返回稳定的提醒组 ID
- 返回中文播报文本
- 返回药品名称和剂量

### 验收标准

```
到点能收到系统通知
能播放中文语音
能震动
未确认时能重复提醒
已服用后停止提醒
退出账号后不再提醒
```

------

## 第 9 阶段：异常处理、安全和测试

负责人：两人共同

### A 重点负责

- 后端 pytest
- JWT 鉴权测试
- 用户数据隔离测试
- AI 服务异常测试
- 数据库约束测试
- CORS 测试

### B 重点负责

- Android 真机测试
- 权限拒绝测试
- 通知测试
- TTS 测试
- 录音测试
- 弱网测试
- Token 失效测试

### 共同验收

代码计划书要求后端至少覆盖健康检查、CORS、手机号校验、验证码登录、JWT 鉴权、用户数据隔离、今日提醒、多药合并、服药记录幂等、AI 返回结构校验等测试项；Android 真机测试也需要覆盖登录、拍照、上传、通知、TTS、震动、录音、前后台和退出账号后是否仍错误提醒。

------

## 第 10 阶段：文档和交付

负责人：两人共同

### A 负责整理

- 后端运行说明
- 数据库初始化说明
- Alembic 迁移说明
- API 文档
- `.env.example`
- 后端测试报告
- ER 图

### B 负责整理

- 前端运行说明
- Android APK 构建说明
- 真机测试报告
- 演示视频
- 页面功能说明
- 权限说明

### 共同完成

- README
- 项目分工说明
- 已知问题和后续改进
- 最终演示流程

项目计划书明确交付物包括前端源代码、后端源代码、数据库迁移文件、环境变量示例、API 文档、运行说明、测试报告、APK、演示视频、ER 图、分工说明和已知问题。

------

# 六、两个人的任务边界表

## A 同学任务边界

| 模块             | A 是否主责 | 说明                        |
| ---------------- | ---------- | --------------------------- |
| 环境搭建         | 是         | 后端环境、数据库配置主导    |
| 登录认证         | 是         | 页面、接口、JWT、用户表     |
| 用户资料         | 是         | 称呼、手机号、时区          |
| 药品管理         | 是         | 药品表、接口、页面          |
| AI 识药          | 是         | 图片上传、AI 调用、人工确认 |
| AI 识别记录      | 是         | 识别日志、错误信息          |
| 我的页面用户信息 | 是         | 用户资料、药品数量          |
| 用药计划         | 配合       | 提供药品数据                |
| 今日提醒         | 配合       | 提供用户称呼和药品信息      |
| Android 通知     | 配合       | 提供播报文本字段            |
| 后端测试         | 是         | 认证、药品、AI、安全        |

------

## B 同学任务边界

| 模块             | B 是否主责 | 说明                      |
| ---------------- | ---------- | ------------------------- |
| 底部导航         | 是         | 今天、识药、计划、我的    |
| 用药计划         | 是         | 计划页面、接口、表结构    |
| 提醒时间         | 是         | 每日次数和时间校验        |
| 今日提醒         | 是         | 今日接口、卡片、多药合并  |
| 服药记录         | 是         | 已服用、跳过、稍后提醒    |
| Android 通知     | 是         | 通知、震动、TTS、重复提醒 |
| 自定义提醒音     | 是         | 录音、试听、删除          |
| 我的页面记录部分 | 是         | 最近服药记录              |
| AI 识药          | 配合       | 识别保存后刷新计划和提醒  |
| 用户资料         | 配合       | 使用称呼生成播报          |
| 真机测试         | 是         | 权限、通知、语音、录音    |

------

# 七、最重要的协作注意事项

## 1. 虽然不按前后端分工，但接口必须统一

业务线分工不是各写各的。
 A 写登录和药品，B 写计划和提醒，但两个人必须共享：

- 接口返回格式
- Token 方式
- 错误码
- 时间格式
- 数据库命名
- 状态枚举

否则最后会出现：

```
A 的接口能跑
B 的页面也能跑
但整个项目联调不起来
```

------

## 2. 每个业务模块必须有“完成标准”

不能说“我写完了”，而要按验收标准检查。

比如 AI 识药模块完成标准不是“接口写完了”，而是：

```
能拍照
能上传
能识别
能展示
能人工修改
能保存药品
能创建计划
能刷新提醒
```

计划提醒模块完成标准不是“计划表写完了”，而是：

```
能创建计划
能生成今日提醒
能合并多药
能到点通知
能标记已服用
能生成记录
```

------

## 3. 数据隔离必须从第一天就做

所有核心表都要有用户归属，或者能通过关联表追溯到用户。

必须隔离的数据包括：

```
药品
计划
提醒时间
服药记录
AI 识别记录
自定义录音
```

不能等项目快做完了再补用户隔离，那时候会很难改。

------

## 4. 先做手动药品，再做 AI 识药

建议开发顺序是：

```
手动新增药品 → 创建计划 → 今日提醒 → 已服用记录 → AI 识药
```

不要一开始就卡在 AI 模型调用上。

原因是：
 AI 识药属于增强入口，本质上是为了减少手工录入。即使 AI 没接好，项目主流程也应该能通过手动新增药品跑通。

------

## 5. 今日提醒接口是全项目核心接口

这个接口连接了：

- 计划模块
- 药品模块
- 用户资料模块
- 服药记录模块
- Android 通知模块
- 中文语音模块

所以 `/api/v1/today` 的返回结构一定要尽早确定。

建议 B 主导设计，A 配合确认字段。

------

## 6. AI 识药必须有 Mock 方案

阿里云百炼视觉模型可能会遇到：

- API Key 未配置
- 网络超时
- 返回格式不稳定
- 额度不足
- 图片识别失败

所以 A 必须提供 mock 模式，例如 `.env` 中配置：

```
AI_RECOGNITION_MODE=mock
```

mock 返回固定数据：

```
{
  "document_type": "药盒",
  "medicines": [
    {
      "name": "阿司匹林",
      "specification": "100mg",
      "dosage": "1片",
      "daily_times": 1,
      "suggested_times": ["08:00"],
      "confidence": 0.88,
      "need_confirm": true
    }
  ]
}
```

这样 B 可以不依赖真实 AI 服务继续开发识药确认页面和保存流程。

------

## 7. Android 真机网络地址不要写 127.0.0.1

真机访问电脑后端不能用：

```
http://127.0.0.1:8000
```

应该使用电脑局域网 IP：

```
http://192.168.x.x:8000
```

后端启动：

```
uvicorn app.main:app --host 0.0.0.0 --port 8000
```

这个问题非常常见，必须写进运行文档。

------

## 8. 修改计划后必须刷新本地通知

这些操作都必须触发本地通知刷新：

```
新增计划
修改计划
删除计划
暂停计划
启用计划
标记已服用
退出登录
```

否则会出现：

```
后端数据已经变了
Android 手机仍然按旧计划提醒
```

这是提醒类项目最容易出错的地方。

------

## 9. 权限申请要按需触发

不要首次打开 App 就申请所有权限。
 正确方式：

| 功能 | 权限申请时机                 |
| ---- | ---------------------------- |
| 拍照 | 点击拍照时                   |
| 相册 | 点击选择图片时               |
| 通知 | 第一次开启提醒时             |
| 录音 | 点击录制自定义提醒音时       |
| 震动 | 不需要明显弹窗，但要声明权限 |

用户拒绝权限时，要有中文说明：

```
需要相机权限才能拍照识别药品，你也可以选择从相册上传图片。
```

------

## 10. 医疗安全提示必须贯穿全流程

至少这些地方要显示安全提示：

- 登录页
- 识药页面
- AI 结果确认页面
- 保存计划前
- 项目说明文档

提示内容可以是：

```
本 App 仅用于用药信息管理和提醒，AI 识别结果可能存在错误，请以医生处方、药品包装和说明书为准。
```

不能出现：

- 自动推荐药品
- 自动修改剂量
- 建议停药
- 建议换药
- 疾病诊断

------

# 八、建议的 Git 协作方式

虽然不按前后端分工，但建议按业务线建分支。

```
main
dev
feature/auth-medicine-ai
feature/plan-reminder-log
feature/docs-test
```

A 使用：

```
feature/auth-medicine-ai
```

B 使用：

```
feature/plan-reminder-log
```

流程建议：

1. 每天开发前先拉取 `dev`
2. 每个小功能完成后提交
3. 每天至少合并一次到 `dev`
4. 合并前先跑通自己的模块
5. 合并后两个人一起测试主流程
6. 稳定版本再合并到 `main`

提交信息建议：

```
feat(auth): add sms login api
feat(medicine): add medicine list page
feat(plan): add create medication plan
fix(today): merge reminders with same time
test(log): add medication log idempotent test
docs: add local development guide
```

------

# 九、最终首版范围建议

两个人时间有限时，首版不要贪多。
 建议按代码计划书里的“基础验收标准”优先做。

## 首版必须完成

| 功能                 | 负责人         |
| -------------------- | -------------- |
| 统一环境和项目骨架   | A 主导，B 配合 |
| 手机号验证码登录     | A              |
| JWT 鉴权             | A              |
| 用户资料             | A              |
| 手动新增药品         | A              |
| 药品列表             | A              |
| 创建用药计划         | B              |
| 计划列表、修改、删除 | B              |
| 今日提醒             | B              |
| 同一时间多药合并     | B              |
| 标记已服用           | B              |
| 服药记录             | B              |
| Android 本地通知     | B              |
| 中文 TTS             | B              |
| 用户数据隔离         | A、B 都要做    |
| 后端基础测试         | A 主导         |
| Android 真机测试     | B 主导         |
| README 和交付文档    | 两人共同       |

## 首版尽量完成

| 功能                     | 负责人         |
| ------------------------ | -------------- |
| AI 图片识药              | A              |
| AI 结果人工确认          | A              |
| 保存识别结果为药品和计划 | A 主导，B 配合 |
| 震动提醒                 | B              |
| 重复提醒                 | B              |
| 自定义录音提醒           | B              |

## 可以后续扩展

| 功能           |
| -------------- |
| 家属账号绑定   |
| 家属远程查看   |
| 日历视图       |
| 周/月统计      |
| 药品库存提醒   |
| 药品有效期提醒 |
| 离线缓存       |
| 断网补传       |
| HTTPS 部署     |
| 开机恢复提醒   |
| 电池优化引导   |

------

# 十、最终总结

这个项目两个人不要按“前端一个、后端一个”分，而要按 **完整业务线** 分。

最推荐的分法是：

```
A 同学：账号登录 + 用户资料 + 药品管理 + AI 识药
B 同学：用药计划 + 今日提醒 + Android 通知语音 + 服药记录
```

两个人共同维护：

```
项目环境
接口规范
数据库规范
时间格式
错误格式
测试
文档
演示
```

最核心的主流程是：

```
登录
  ↓
新增药品 / AI 识药
  ↓
创建用药计划
  ↓
今天页面展示提醒
  ↓
Android 到点通知 + 语音
  ↓
用户标记已服用
  ↓
生成服药记录
```

开发时一定要优先保证这条主流程跑通。然后再完善 AI 识药、自定义录音、稍后提醒、统计和家属绑定等提高功能。