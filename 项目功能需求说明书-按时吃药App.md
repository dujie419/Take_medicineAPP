# “按时吃药”Android App 项目功能需求说明书

## 1. 项目概述

### 1.1 项目名称

按时吃药（Take Medicine On Time）

### 1.2 项目定位

开发一款主要运行在 Android 手机上的用药管理应用，面向老年用户及需要长期服药的人群。应用通过手机拍照识别药盒、处方或用药清单，帮助用户建立用药计划，并通过系统通知、震动和中文语音进行按时提醒。

### 1.3 核心目标

1. 降低用户手工录入药品信息的难度。
2. 帮助用户清楚地查看每日服药安排。
3. 在约定时间通过 Android 通知、震动和语音提醒用户服药。
4. 记录用户的服药操作，方便查看历史执行情况。
5. 保证识药结果必须经过人工确认，避免将 AI 识别结果直接视为医疗建议。

### 1.4 目标用户

- 老年用户；
- 慢性病、长期服药用户；
- 需要帮助家人设置用药计划的家庭成员；
- 需要管理多种药品、多个服药时间的用户。

## 2. 项目范围

项目至少包含以下部分：

- Android App 前端；
- 后端 RESTful API；
- 关系型数据库；
- 手机号验证码登录；
- AI 图片识药服务；
- Android 本地通知、震动、语音及录音能力；
- 基础自动化测试；
- 可运行的部署或本地联调方案。

推荐技术方案如下，但允许学生在满足需求的前提下调整：

- 前端：uni-app + Vue 3；
- Android 运行环境：App-Plus / APK；
- 后端：FastAPI；
- 数据库：MySQL；
- ORM：SQLAlchemy；
- 数据库迁移：Alembic；
- 身份认证：JWT；
- 短信服务：阿里云短信或等价服务；
- 图片识别：支持视觉能力的大模型 API。

## 3. 用户角色与权限

### 3.1 普通用户

普通用户可以：

- 获取短信验证码；
- 使用手机号和验证码登录；
- 修改自己的称呼；
- 拍照或从相册选择药品图片；
- 查看并修改 AI 识别结果；
- 保存自己的药品；
- 新增、查看、修改、删除自己的用药计划；
- 查看当天的服药提醒；
- 标记药品已服用；
- 查看自己的服药记录；
- 录制、试听、删除自定义提醒音；
- 退出登录。

### 3.2 数据权限

- 用户只能访问自己的药品、计划、提醒和服药记录。
- 用户不能通过修改请求参数访问其他用户的数据。
- 所有需要登录的接口都必须验证 JWT。
- 用户被停用后不得继续登录或访问受保护资源。

## 4. 核心业务流程

### 4.1 首次使用流程

1. 用户打开 App。
2. 输入中国大陆 11 位手机号。
3. 点击“获取验证码”。
4. 输入 6 位短信验证码。
5. 验证成功后，系统自动创建用户账号。
6. 系统保存登录状态并进入“今天”页面。

### 4.2 再次登录流程

1. 用户输入已注册手机号。
2. 获取并输入短信验证码。
3. 验证成功后获取新的访问令牌。
4. App 保存用户信息和令牌。
5. 进入“今天”页面并同步当天提醒。

### 4.3 拍照识药流程

1. 用户进入“识药”页面。
2. 用户调用相机拍照，或从相册选择图片。
3. App 压缩并上传图片。
4. 后端校验图片类型、大小和登录状态。
5. 后端调用 AI 视觉识别服务。
6. AI 返回一个或多个药品的结构化信息。
7. 前端展示识别结果和可信度。
8. 用户逐项核对并修改药名、规格、剂量、次数、时间和说明。
9. 用户可以删除误识别药品，也可以手工补充漏识别药品。
10. 用户确认后，系统保存药品及对应的用药计划。

### 4.4 用药提醒流程

1. 后端根据用户时区、有效计划和提醒时间生成当天提醒。
2. 同一时间需要服用的多个药品应合并为一组提醒。
3. App 同步当天提醒并设置 Android 本地通知。
4. 到达提醒时间后，App 发出系统通知、震动和中文语音。
5. 用户未标记“已服用”时，可按固定间隔继续提醒。
6. 用户进入“今天”页面确认已服用。
7. App 为该组提醒中的每个计划分别保存服药记录。
8. 已完成提醒不再继续重复通知。

## 5. 前端功能需求

## 5.1 登录页面

### 页面内容

- 应用名称及简短说明；
- 手机号输入框；
- 短信验证码输入框；
- “获取验证码”按钮；
- 验证码倒计时；
- “登录 / 首次自动注册”按钮；
- 医疗安全提示。

### 功能要求

- 手机号必须符合中国大陆手机号格式：11 位，以 1 开头，第二位为 3～9。
- 验证码必须为 6 位数字。
- 获取验证码后进入倒计时，倒计时期间不可重复获取。
- 发送验证码和登录过程中，按钮应进入加载或禁用状态，防止重复提交。
- 首次登录自动创建账号，不单独提供注册页面。
- 已登录用户再次打开登录页时，应自动进入“今天”页面。
- 登录成功后保存 JWT 和用户信息。
- JWT 失效或接口返回 401 时，应清除本地登录信息并返回登录页。
- 网络失败、超时、验证码错误等情况应给出清晰提示。

## 5.2 底部主导航

登录后提供四个主要入口：

1. 今天；
2. 识药；
3. 计划；
4. 我的。

导航要求：

- 当前页面应有明显选中状态；
- 按钮和文字应适合老年用户点击与阅读；
- 底部导航应避开 Android 全面屏安全区域；
- 页面切换后不应出现重复页面堆栈。

## 5.3 “今天”页面

### 页面内容

- 根据当前时间显示“早上好、下午好”等问候语；
- 显示并允许修改用户称呼；
- 显示当天日期和星期；
- 显示今日提醒总数、已完成数、待完成数；
- 显示完成进度；
- 显示自动语音提醒状态；
- 按时间顺序展示当天提醒；
- 支持下拉刷新和手动刷新。

### 提醒卡片

每组提醒至少展示：

- 提醒时间；
- 上午、下午、晚上等时间段；
- 药品数量；
- 每种药品名称；
- 每次服用量；
- 是否已经服用；
- “已服用”操作按钮；
- 自定义提醒音相关操作。

### 同时服用多种药品

- 多个计划提醒时间相同时，应合并为一张卡片。
- 卡片中列出该时刻需要服用的全部药品。
- 用户点击“已服用”时，应明确提示这是对整组药品进行确认。
- 后端仍需为每个计划分别保存服药记录。
- 只有整组药品都已完成时，该组提醒才显示为“已服用”。

### 修改称呼

- 用户可点击自己的称呼进行编辑。
- 称呼不能为空。
- 输入后可采用防抖方式自动保存。
- 保存成功后应更新本地用户信息。
- 提醒语音中的称呼也应同步更新。

### 已服用确认

- 点击“已服用”后应弹出二次确认框。
- 弹窗应列出本组药品及服用量。
- 确认后创建服药记录。
- 成功后立即更新页面状态并取消后续重复提醒。

## 5.4 “识药”页面

### 图片选择

- 支持调用手机相机拍照。
- 支持从手机相册选择图片。
- 每次至少支持一张图片。
- 应优先使用压缩图片，减少上传耗时。
- 应申请并处理 Android 相机权限。
- 用户取消选择图片时，页面不能报错。

### 图片预览

- 展示已选择图片。
- 支持重新拍照或重新选择。
- 支持点击“开始识别”上传图片。
- 识别过程中显示明确加载状态，防止重复上传。

### AI 识别范围

系统应支持识别：

- 单个药盒或药瓶；
- 多个药品；
- 医生处方；
- 用药清单；
- 无法确定类型的药品相关图片。

### AI 结构化结果

每种药品尽量返回：

- 药品中文名称；
- 药品规格；
- 每次服用量；
- 每日服用次数；
- 建议提醒时间；
- 服用说明；
- 识别可信度；
- 风险提示或待确认事项；
- 是否需要人工确认。

整张图片还应返回：

- 文档类型；
- 药品列表；
- 整体风险提示。

### 人工确认

- AI 结果不能直接保存，必须先展示给用户确认。
- 用户可以修改所有关键字段。
- 用户可以删除误识别项目。
- 用户可以添加漏识别药品。
- 药品名称不能为空。
- “无法可靠识别”不能直接作为最终药品名称保存。
- 每次服用量应由用户确认后再保存。
- 每日次数建议限制在合理范围内，前端至少支持 1～6 次。
- 提醒时间数量必须与每日服用次数一致。

### 默认提醒时间

可采用以下默认规则：

- 每日 1 次：08:00；
- 每日 2 次：08:00、19:00；
- 每日 3 次：08:00、12:00、19:00；
- 更多次数：按合理间隔自动生成，用户仍可修改。

### 批量保存

- 用户确认后，逐项保存药品。
- 每保存一个药品，同时创建对应的用药计划。
- 保存完成后刷新 Android 提醒计划。
- 成功后提示保存数量，并允许跳转到“今天”页面。
- 保存过程中出现部分失败时，应避免用户误以为全部成功；建议设计事务、补偿或明确的失败提示。

## 5.5 “计划”页面

### 页面内容

- 展示用户全部用药计划；
- 显示药品名称；
- 显示每次服用量；
- 显示每日服用次数；
- 显示全部提醒时间；
- 显示开始日期；
- 显示计划状态：提醒中或已暂停；
- 支持下拉刷新。

### 页面操作

- 新增计划；
- 修改计划；
- 删除计划；
- 无计划时引导用户先拍照识药。

### 删除计划

- 删除前必须二次确认。
- 提示删除后不再提醒对应药品。
- 删除成功后刷新列表及本地通知安排。

## 5.6 新增/编辑计划页面

### 表单字段

- 药品；
- 每日服用次数；
- 每次提醒时间；
- 开始日期；
- 结束日期，可为空；
- 备注；
- 是否开启提醒。

### 业务规则

- 新增计划时，只能选择当前用户已经保存的药品。
- 编辑已有计划时，药品不允许随意更换；如需更换，可删除后重建。
- 每日服用次数与提醒时间数量必须一致。
- 同一计划的提醒时间不能重复。
- 结束日期不能早于开始日期。
- 结束日期为空表示长期有效。
- 暂停计划后，该计划不应出现在有效提醒中。
- 计划保存成功后立即刷新 Android 本地通知。

## 5.7 “我的”页面

### 用户信息

- 显示用户称呼；
- 显示脱敏或完整手机号；
- 显示用户时区，默认中国标准时间。

### 数据统计

- 已保存药品数量；
- 服药记录数量。

### 我的药品

- 展示用户保存的药品列表；
- 展示药品名称；
- 展示每次服用量；
- 无药品时显示空状态；
- 提供添加药品入口。

### 最近服药记录

- 展示最近若干条记录；
- 显示记录状态；
- 显示计划编号或药品信息；
- 显示计划服药时间；
- 状态至少包含：已服用、已跳过、稍后提醒。

### 退出登录

- 退出前二次确认。
- 清除本地 JWT 和用户信息。
- 停止前台提醒轮询。
- 清除本地已安排通知。
- 返回登录页面。

## 6. Android 专属功能需求

## 6.1 系统权限

根据功能申请并正确处理以下权限：

- 网络访问；
- 网络状态；
- 相机；
- 相册或媒体访问（按 Android 版本适配）；
- 录音；
- Android 13 及以上通知权限；
- 震动；
- 唤醒设备；
- 开机完成事件（如实现开机恢复提醒）。

要求：

- 权限应在需要时申请，不应无理由一次性索取全部敏感权限。
- 用户拒绝权限时，应给出说明和替代操作。
- 用户选择“不再询问”时，应提示其前往系统设置开启。

## 6.2 本地通知

- 根据当天提醒生成 Android 本地通知。
- 通知标题应清晰，例如“按时吃药提醒”。
- 通知正文包含当前应服用的药品。
- 点击通知后进入“今天”页面。
- 已服用或失效的提醒不得继续通知。
- 修改、删除、暂停计划后应重新安排通知。
- 退出登录后清除相关通知。

## 6.3 震动和重复提醒

- 到点时触发震动。
- 用户未确认服药时，可每 5 分钟重复提醒一次。
- 重复提醒应设置最长有效窗口，例如 2 小时。
- 已确认服药后立即停止重复提醒。
- 应避免同一个提醒因前台轮询和系统通知同时触发而连续播放多次。

## 6.4 中文语音播报

- Android 端使用系统 TTS 播放中文提醒。
- 播报内容应包含用户称呼、时间和药品名称。
- 示例：“张阿姨，现在上午八点了，该吃阿司匹林、二甲双胍了。”
- 建议适当降低语速，方便老年用户听清。
- 系统 TTS 不可用时，应至少保留通知文字和 Toast 提示。

## 6.5 自定义提醒音

- 用户可针对某一组提醒录制自定义声音。
- 首次录音时申请麦克风权限。
- 支持开始和停止录音。
- 录音过短时提示重新录制。
- 将录音保存到 App 本地持久目录。
- 支持试听、重新录制和删除。
- 删除后恢复使用系统中文 TTS。
- 自定义录音按“用户 + 提醒组”隔离，不能串到其他账号。

## 6.6 前后台行为

- App 进入前台时启动提醒检查并同步当天计划。
- App 进入后台时停止不必要的高频轮询。
- 后台提醒主要依赖系统通知机制。
- 应考虑 Android 系统省电策略对后台任务的影响。
- 可选提高项：实现开机后恢复提醒和跨天自动重建提醒。

## 7. 后端功能需求

## 7.1 通用要求

- 提供 RESTful JSON API。
- API 建议统一使用 `/api/v1` 前缀。
- 提供健康检查接口。
- 提供 OpenAPI/Swagger 文档。
- 正确使用 HTTP 状态码。
- 返回统一、可读的错误信息。
- 支持前端本地调试所需的 CORS。
- 所有数据库操作必须保证用户数据隔离。

## 7.2 短信验证码

### 获取验证码

- 接收手机号。
- 校验手机号格式。
- 生成 6 位随机验证码。
- 验证码不能以明文形式存入数据库，需保存摘要。
- 记录用途、过期时间、使用状态、错误次数和创建时间。
- 调用短信服务发送验证码。
- 短信服务失败时返回明确错误。

### 安全与限流

- 验证码有效期建议 5 分钟。
- 同一手机号两次发送间隔建议不少于 60 秒。
- 限制单个手机号每日发送次数，例如 10 次。
- 限制验证码错误尝试次数，例如 5 次。
- 验证成功后立即标记为已使用，不可重复使用。
- 开发环境可以使用固定测试验证码，但生产环境不得返回验证码。

## 7.3 登录与认证

- 手机号和验证码验证成功后登录。
- 首次登录自动创建用户。
- 已存在用户直接登录。
- 返回 JWT 和用户信息。
- JWT 应包含用户标识和过期时间。
- JWT 密钥不得硬编码为生产默认值。
- 受保护接口必须校验 Bearer Token。
- 无效、过期或对应用户不存在的 Token 返回 401。
- 停用用户返回 403。

## 7.4 用户资料

- 获取当前用户资料。
- 修改用户称呼。
- 称呼去除首尾空格后不能为空。
- 用户时区默认为 `Asia/Shanghai`。

## 7.5 药品管理

- 新增药品；
- 查询当前用户药品列表；
- 查询当前用户某个药品；
- 药品按创建时间倒序返回；
- 不得查询其他用户药品。

药品字段至少包括：

- ID；
- 用户 ID；
- 名称；
- 规格；
- 每次服用量；
- 服用说明；
- 原始图片路径；
- AI 识别可信度；
- 创建时间；
- 更新时间。

## 7.6 AI 药品识别

### 上传校验

- 仅允许 JPEG、PNG、WebP。
- 拒绝空文件。
- 限制文件大小，例如最大 10 MB。
- 文件名使用随机 ID，避免覆盖和路径注入。
- 图片按用户目录隔离存储。

### AI 调用

- 将图片发送给支持视觉识别的大模型。
- 提示模型使用简体中文输出。
- 英文药盒应尽量转换为中国大陆常用中文药名。
- 要求模型输出严格的结构化 JSON。
- 对模型返回内容进行服务端校验。
- 识别结果最多包含合理数量的药品，例如 30 种。
- 解析失败、模型不可用、超时等情况应返回明确错误。

### 识别记录

每次识别应记录：

- 用户 ID；
- 图片路径；
- 模型名称；
- 结构化结果；
- 原始响应；
- 错误信息；
- 创建时间。

### 医疗安全

- 识别结果只用于信息提取和提醒设置。
- 前后端均应提示用户对照处方、药盒或说明书确认。
- 系统不得自动给出诊断、处方调整或停药建议。

## 7.7 用药计划管理

提供以下能力：

- 创建计划；
- 查询计划列表；
- 查询计划详情；
- 修改计划；
- 删除计划。

计划字段至少包括：

- ID；
- 用户 ID；
- 药品 ID；
- 每日次数；
- 开始日期；
- 结束日期；
- 备注；
- 是否启用；
- 提醒时间列表；
- 创建时间；
- 更新时间。

业务校验：

- 药品必须属于当前用户。
- 每日次数为 1～12 次。
- 提醒时间数量必须等于每日次数。
- 同一计划的提醒时间不能重复。
- 提醒时间应按时间顺序返回。
- 结束日期不得早于开始日期。
- 修改计划时必须保持次数与时间数量一致。
- 删除计划时级联删除提醒时间和服药记录，或采用等价的一致性方案。

## 7.8 今日提醒

后端根据以下条件生成当天提醒：

- 计划属于当前用户；
- 计划处于启用状态；
- 开始日期不晚于今天；
- 结束日期为空或不早于今天；
- “今天”必须按用户时区计算。

返回内容至少包括：

- 组合提醒 ID；
- 计划 ID 列表；
- 药品列表；
- 计划提醒时间；
- 中文播报文案；
- 当前服药状态。

组合规则：

- 同一用户、同一天、同一时间的多个计划合并为一组。
- 药品可按计划 ID 或稳定规则排序。
- 中文播报应包含用户称呼和全部药品名称。
- 返回结果按提醒时间升序排列。

## 7.9 服药记录

支持创建或更新服药记录、查询历史记录。

记录字段至少包括：

- ID；
- 计划 ID；
- 计划服药时间；
- 实际操作时间；
- 状态；
- 稍后提醒时间；
- 备注；
- 创建时间；
- 更新时间。

状态至少包括：

- `taken`：已服用；
- `skipped`：已跳过；
- `snoozed`：稍后提醒。

业务规则：

- 计划必须属于当前用户。
- 同一计划、同一计划服药时间只能有一条记录。
- 重复提交时应更新原记录，保证操作幂等。
- 未提供实际操作时间时由后端填入当前时间。
- 选择“稍后提醒”但未提供时间时，可默认延后 10 分钟。
- 非“稍后提醒”状态不应保存无效的稍后提醒时间。
- 历史记录按计划服药时间倒序返回。

## 8. 数据库设计要求

至少包含以下数据表：

1. 用户表 `users`；
2. 短信验证码表 `sms_verification_codes`；
3. 药品表 `medicines`；
4. 用药计划表 `medication_plans`；
5. 提醒时间表 `reminder_times`；
6. 服药记录表 `medication_logs`；
7. AI 识别记录表 `ai_recognition_records`。

关键约束：

- 用户手机号唯一；
- 同一计划的同一提醒时间唯一；
- 同一计划与同一计划服药时间的日志唯一；
- 主要外键应建立索引；
- 用户删除后，其药品和计划应按设计级联处理；
- 计划删除后，其提醒时间和日志应保持数据一致性；
- 所有主要业务表应记录创建和更新时间。

## 9. API 清单

建议至少实现以下接口：

| 方法 | 路径 | 功能 | 是否登录 |
| --- | --- | --- | --- |
| GET | `/health` | 服务健康检查 | 否 |
| POST | `/api/v1/auth/sms-codes` | 获取短信验证码 | 否 |
| POST | `/api/v1/auth/login` | 验证码登录/自动注册 | 否 |
| GET | `/api/v1/auth/me` | 获取当前用户 | 是 |
| PATCH | `/api/v1/auth/me` | 修改用户称呼 | 是 |
| POST | `/api/v1/medicines/recognize` | 上传图片并识别药品 | 是 |
| GET | `/api/v1/medicines` | 查询药品列表 | 是 |
| POST | `/api/v1/medicines` | 新增药品 | 是 |
| GET | `/api/v1/medicines/{id}` | 查询药品详情 | 是 |
| GET | `/api/v1/plans` | 查询用药计划 | 是 |
| POST | `/api/v1/plans` | 新增用药计划 | 是 |
| GET | `/api/v1/plans/{id}` | 查询计划详情 | 是 |
| PUT | `/api/v1/plans/{id}` | 修改用药计划 | 是 |
| DELETE | `/api/v1/plans/{id}` | 删除用药计划 | 是 |
| GET | `/api/v1/today` | 查询当天组合提醒 | 是 |
| GET | `/api/v1/medication-logs` | 查询服药记录 | 是 |
| POST | `/api/v1/medication-logs` | 创建或更新服药记录 | 是 |

## 10. 网络与错误处理

### 10.1 前端请求

- API 基础地址必须通过环境变量配置。
- Android 真机不能把 `127.0.0.1` 当作电脑后端地址。
- 使用局域网后端时，App 应配置电脑局域网 IP，后端监听 `0.0.0.0`。
- 使用公网服务时推荐 HTTPS。
- 如开发阶段使用 HTTP，需正确配置 Android 明文网络策略。
- 普通请求建议设置 15 秒左右超时。
- 图片识别上传建议设置更长超时，例如 90 秒。

### 10.2 错误提示

至少处理：

- 网络不可用；
- 请求超时；
- 服务端不可用；
- 参数校验失败；
- 登录过期；
- 无权限；
- 数据不存在；
- 验证码错误或过期；
- 请求过于频繁；
- 图片格式错误；
- 图片过大；
- AI 服务不可用；
- 保存计划失败。

错误提示应使用用户可理解的中文，不直接展示堆栈或数据库异常。

## 11. 非功能需求

## 11.1 易用性

- 面向老年用户，字号、按钮和点击区域应偏大。
- 页面层级尽量简单。
- 重要操作必须有明显反馈。
- 表单输入应提供合理默认值。
- 空状态应告诉用户下一步做什么。
- 高风险操作应二次确认。

## 11.2 性能

- 常规 API 请求应在合理时间内完成。
- 图片上传前进行压缩。
- 列表查询避免明显的 N+1 数据库问题。
- 当天提醒同步不应产生高频无意义请求。
- 前台轮询应在页面或 App 隐藏后停止。

## 11.3 安全

- 生产环境必须使用足够强的 JWT 密钥。
- 短信验证码保存摘要，不保存明文。
- 生产日志不得输出验证码、Token、短信密钥或 AI API Key。
- 上传文件必须校验类型、大小和存储路径。
- 所有资源查询都必须带当前用户过滤条件。
- 数据库、短信和 AI 密钥通过环境变量配置。
- CORS 不应在生产环境中无条件允许所有来源。

## 11.4 数据一致性

- 创建药品和计划时应考虑部分成功问题。
- 重复点击“已服用”不能生成重复日志。
- 修改计划后本地通知与后端数据必须一致。
- 删除或暂停计划后不得继续提醒。
- 跨时区和跨日期计算应统一规则。

## 11.5 兼容性

- 项目必须能构建为 Android App。
- 至少适配常见 Android 屏幕尺寸。
- 适配全面屏安全区域和状态栏。
- 适配 Android 13 以上通知权限。
- 对相机、录音、通知权限拒绝情况进行测试。

## 12. 测试要求

## 12.1 后端自动化测试

至少覆盖：

- 健康检查；
- CORS；
- 手机号格式校验；
- 验证码登录；
- JWT 鉴权；
- 用户数据隔离；
- 药品新增和查询；
- 计划新增、修改、删除；
- 重复提醒时间校验；
- 结束日期校验；
- 今日提醒生成；
- 同一时间多药合并；
- 服药记录幂等；
- 用户时区计算；
- AI 返回结构校验；
- AI 服务异常处理。

## 12.2 Android 真机测试

至少验证：

- 首次安装权限流程；
- 手机号登录；
- 登录状态持久化；
- 相机拍照；
- 相册选图；
- 图片上传；
- AI 识别结果展示；
- 药品和计划保存；
- 到点通知；
- 通知点击跳转；
- 中文语音；
- 震动；
- 自定义录音；
- 前后台切换；
- 断网和弱网；
- Token 失效；
- 修改、暂停和删除计划后通知是否正确更新；
- 退出账号后是否仍会错误提醒。

## 13. 基础验收标准

学生提交项目应达到以下最低标准：

1. Android App 能正常安装和启动。
2. 手机号验证码登录流程可用。
3. 前后端能够通过真实网络地址联调。
4. 用户可以拍照或选图并完成药品识别。
5. AI 结果可以人工修改后保存。
6. 用户可以新增、查询、修改、删除用药计划。
7. “今天”页面能正确展示当天提醒。
8. 同一时间的多种药品能合并显示。
9. Android 到点能够产生通知，并能播放中文语音或替代提醒。
10. 用户能标记已服用，并生成服药记录。
11. 不同用户之间数据严格隔离。
12. 关键参数有前后端双重校验。
13. 网络、权限和第三方服务异常有合理提示。
14. 后端具有基础自动化测试。
15. 项目附带完整运行说明、环境变量示例和数据库初始化方式。

## 14. 建议提交物

每组学生应提交：

- 前端源代码；
- 后端源代码；
- 数据库迁移文件；
- 环境变量示例文件；
- API 文档；
- 项目运行说明；
- 测试报告；
- Android APK；
- 演示视频；
- 数据库设计图或 ER 图；
- 项目分工说明；
- 已知问题和后续改进说明。

不得提交：

- 真实短信密钥；
- 真实 AI API Key；
- 生产数据库密码；
- 用户隐私数据；
- 包含有效 Token 的日志。

## 15. 可选提高功能

完成基础要求后，可以选择实现：

- “稍后提醒”和“跳过本次”前端操作；
- 药品编辑和删除；
- 计划服药完成率统计；
- 日历视图和周/月统计；
- 家属账号与老人账号绑定；
- 家属远程查看服药状态；
- 漏服告警；
- 多设备同步；
- 药品库存和余量提醒；
- 药品有效期提醒；
- OCR 与大模型组合识别；
- 图片裁剪、旋转和清晰度检测；
- 离线缓存；
- 断网后服药记录补传；
- HTTPS 和域名部署；
- Docker 一键部署；
- 开机后自动恢复提醒；
- Android 电池优化引导；
- 消息推送服务；
- 无障碍适配；
- 普通话以外的方言或家人录音提醒。

## 16. 评分参考

| 模块 | 建议占比 |
| --- | ---: |
| 需求理解与产品完整性 | 10% |
| Android 前端页面与交互 | 20% |
| 后端 API 与数据库设计 | 20% |
| AI 图片识药及人工确认 | 15% |
| 提醒、通知、语音和录音 | 15% |
| 安全、异常处理与数据隔离 | 8% |
| 自动化测试与真机测试 | 7% |
| 文档、部署和演示 | 5% |

## 17. 医疗安全声明

本项目是用药信息管理和提醒工具，不是医疗诊断或处方系统。

- AI 识别结果可能存在错误；
- 用户必须根据医生处方、药品包装和说明书进行核对；
- 系统不得擅自建议增加、减少、更换或停止药物；
- 对无法可靠识别的信息，应明确标记并要求人工确认；
- 涉及真实用户时，应遵守隐私保护和数据安全要求。

